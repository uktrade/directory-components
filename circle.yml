machine:
  python:
    version: 3.6.3

dependencies:
  override:
    - make test_requirements

test:
  override:
    - make test

deployment:
  build_package:
    branch: master
    commands:
      - make publish
